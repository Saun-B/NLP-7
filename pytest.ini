[pytest]
testpaths = tests
python_files = test_*.py
addopts = -ra --strict-markers --basetemp=.pytest_tmp
markers =
    slow: needs the PhoBERT tokenizer/model, or launches a server (network or HF cache)
    integration: loads real trained checkpoints / artifacts from outputs/
